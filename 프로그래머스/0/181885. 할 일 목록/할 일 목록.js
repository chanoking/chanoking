// const solution = (todo_list, finished) => {
//     const arr = {};
//     todo_list.forEach((a)=> arr[a] = finished.forEach((b) => b) )
//     return arr;
// }    

const solution = (todo_list, finished) => {
    const arr = {};
    
    todo_list.forEach((a) => arr[a] = (...finished.forEach((a) => a)))
    
    return arr;
}