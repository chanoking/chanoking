// const solution = (arr) => {
//     const result = [];
//     arr.forEach((e) => )
//     while(return arr.forEach((e) => e.length) > 0){
//         result.push(e)
//     }
//     return result;
// // }

// const solution = (arr) => {
//     const newArr = arr.map((e) => arr.fill(e))
//     return [...newArr]
// }

const solution = (arr) => {
    const answer = [];
    answer.length = 4
    answer.fill(4)
    return answer;
    // for(let i = 0; i < arr.reduce((acc, curr) => acc + curr); i++){
    //     // return [].fill(arr[i])
    //     // answer.push(...[].fill(arr[i]))
    // }
    // return answer;
}